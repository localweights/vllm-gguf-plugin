# Qwen3.5 GDN module inventory — LinearBase verdicts

Generated by `tools/introspect_gdn_modules.py` (WS1 D3 / R3). Verdicts are computed with `issubclass(cls, LinearBase)` against the real vLLM classes that `QwenGatedDeltaNetAttention.__init__` constructs.

## Per-parameter verdict

| GDN param | vLLM class | LinearBase? | GGUF quant handling |
|---|---|---|---|
| `conv1d` | `ColumnParallelLinear` | YES | quantized — picks up `GGUFLinearMethod` |
| `in_proj_qkvz` | `MergedColumnParallelLinear` | YES | quantized — picks up `GGUFLinearMethod` |
| `in_proj_ba` | `MergedColumnParallelLinear` | YES | quantized — picks up `GGUFLinearMethod` |
| `out_proj` | `RowParallelLinear` | YES | quantized — picks up `GGUFLinearMethod` |
| `dt_bias` | `Parameter` | no | dense — high-precision param, dequant-on-load |
| `A_log` | `Parameter` | no | dense — high-precision param, dequant-on-load |
| `norm` | `RMSNormGated` | no | dense — high-precision param, dequant-on-load |

`conv1d` is a `ColumnParallelLinear` (LinearBase) even though it implements the causal convolution — its weight is reshaped and loaded via a mamba sharded loader; GGUF stores `ssm_conv1d` typically at F16/F32 so no low-bit quant applies in practice.

## GGUF GDN-block tensor → vLLM param (preliminary; finalised in T-6)

| GGUF tensor(s) | vLLM param |
|---|---|
| `ssm_conv1d.weight` | conv1d |
| `attn_qkv.weight + attn_gate.weight` | in_proj_qkvz (merged qkv + z gate) |
| `ssm_alpha.weight + ssm_beta.weight` | in_proj_ba (merged b + a) |
| `ssm_out.weight` | out_proj |
| `ssm_dt.bias` | dt_bias |
| `ssm_a` | A_log |
| `ssm_norm.weight` | norm |

The GGUF splits the GDN input projection across `attn_qkv` + `attn_gate` (→ `in_proj_qkvz`) and `ssm_alpha` + `ssm_beta` (→ `in_proj_ba`), while vLLM merges each pair into one `MergedColumnParallelLinear`. Reconciling this split→merged layout is the T-6 tensor-map task.

## Config context

Derived from a `qwen35` metadata fixture: 
linear_num_key_heads=16, linear_num_value_heads (inner/state)=48, linear_key/value_head_dim (state_size)=128, linear_conv_kernel_dim=4
